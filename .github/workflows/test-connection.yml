name: 'Test Huawei Cloud Connection'

on:
  workflow_dispatch:

jobs:
  test-connection:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Authenticate to Huawei Cloud
        id: auth
        uses: huaweicloud/auth-action@v1.0.0
        with:
          access_key_id: ${{ secrets.ACCESSKEY }}
          secret_access_key: ${{ secrets.SECRETACCESSKEY }}
          region: 'la-north-2'
        env:
          LANG: 'en_US.UTF-8'

      - name: Test OBS connection (List buckets)
        uses: huaweicloud/obs-helper@v1.4.0
        with:
          bucket_name: 'un-bucket-cualquiera-no-importa'
          operation_type: 'download'
          obs_file_path: '/'
          local_file_path: '/tmp/ignore-this'

      - name: Connection successful
        if: success()
        run: |
          echo '✅ ✅ ✅ CONEXIÓN EXITOSA ✅ ✅ ✅'
          echo 'Las credenciales ACCESSKEY y SECRETACCESSKEY son válidas.'
          echo 'La autenticación con Huawei Cloud IAM funciona correctamente.'
          echo ''
          echo '🎯 Puedes proceder a configurar el workflow de deploy completo.'